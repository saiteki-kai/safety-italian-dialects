[project]
name = "benchmarks"
version = "0.1.0"
description = "Translation of benchmarks into Italian dialects"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "googletrans-python>=0.2.0",
]
